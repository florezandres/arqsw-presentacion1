# gRPC and HTTP ports (different from Product service)
GRPC_PORT=50052
HTTP_PORT=8082

# SQLite database paths
SQLITE_COMMAND_DB_PATH=./sale_command.db
SQLITE_QUERY_DB_PATH=./sale_query.db

# Kafka configuration (can be same as Product service)
KAFKA_BROKERS=localhost:9092